<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>本のリスト</title>
</head>
<body>
  <h1>本のリスト</h1>
  <table>
    <% for( let row of data ) { %>
      <tr>
        <td><%= row.id %></td>
        <td><a href="/book_db/<%= row.id %>"><%= row.name %></a></td>
      </tr>
    <% } %>
  </table>
</body>
</html> 